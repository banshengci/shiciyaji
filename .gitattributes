# 统一换行符为 LF
# 关键：Windows 上 Git 默认 core.autocrlf=true 会把 LF 转成 CRLF，
# 导致 tools/build_ios_ipa.sh 在 macOS/Linux 上执行时报
# "bad interpreter: /bin/bash^M" 或 "\r: command not found"。
# 这里强制文本文件以 LF 入库，保证 shell 脚本在任意平台可执行。
* text=auto eol=lf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.icns binary
*.keystore binary
*.jks binary
*.ttf binary
*.otf binary
